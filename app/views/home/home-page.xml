<Page loaded = "loaded">

    <ActionBar title="Login" class="action-bar"></ActionBar>

     <FlexboxLayout>
        <StackLayout id="form" orientation="vertical">

            <TextField class="input input-border" id="email" text="{{ email }}"
                hint="Email Address" keyboardType="email" autocorrect="false"
                autocapitalizationType="none" />
            <TextField class="input input-border" secure="true" text="password" id = "password"
                hint="Password" />

            <Button class="btn btn-primary" text="Login" 
                tap="LogIn" />
            <Button text="sign In" class="btn btn-primary" tap="signIn"
                class="link" />
            <Button text="sign out" class="btn btn-primary" tap="SignOut"
                class="link" />
        </StackLayout>
        <StackLayout class="input-field" visibility="{{ !isLoggingIn ? 'visible' : 'collapse' }}">
				<TextField id="confirmPassword" class="input" text="{{ confirmPassword }}" hint="Confirm password" secure="true" returnKeyType="done"
				/>
				<Label class="hr-light" />
			</StackLayout>

    </FlexboxLayout>
</Page>

  

