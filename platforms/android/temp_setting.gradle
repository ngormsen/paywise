android {
            defaultConfig {
                applicationId = "org.nativescript.test2"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }