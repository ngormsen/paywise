android {
            defaultConfig {
                applicationId = "com.goethe.paywise"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }