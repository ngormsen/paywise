<Page xmlns="http://www.nativescript.org/tns.xsd" xmlns:Barcode="nativescript-barcodescanner"  loaded="pageLoaded">
    <ActionBar title="Barcode-Scanner" class="action-bar"></ActionBar>

    <ScrollView>  
    <StackLayout class="tab-content">
        <Label class="h1" textWrap="true" text="Willkommen!"/>
        <Label class="h3" textWrap="true" text="Bitte scanne den Barcode an deinem Tisch."/>

        <!--iOS>
        <ContentView height="240" width="240">
            <Barcode:BarcodeScannerView></Barcode:BarcodeScannerView>
        </ContentView>
        </iOS-->

            <Button class="btn btn-primary btn-rounded-sm" text="Kamera aktivieren" tap="onTap" />
        <iOS>
            <Button class="btn btn-primary btn-rounded-sm" text="back camera, with torch" tap="{{ doScanWithTorch }}" />
        </iOS>
    
    </StackLayout>
    </ScrollView>

</Page>
